root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/server"
  bin = "./tmp/main"
  include_ext = ["go"]
  exclude_dir = ["migrations", "api"]
  delay = 1000
  stop_on_error = true
  send_interrupt = true

